<article>
  <h2>Archive</h2>
  <ul>
    {{ #for post in newest "unlimited" posts }}
    <li>
      <a href="{{site.url}}/{{post.path}}">{{ post.title }}</a> ({{ formatDate post.date }})
    </li>
    {{ #endfor }}
  </ul>
</article>
<section>
  <a href="{{site.url}}/index.html">Return to home.</a>
</section>
